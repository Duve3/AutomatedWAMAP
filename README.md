# AutomatedWAMAP
Automates WAMAP-based mathematical assignments via selenium and AI

The design idea here is to use selenium to automatically login to WAMAP,
go into an assignment (via link), then use some kind of AI for automated answers.

currently it does answer every question, but fails to input in WAMAP
I am not sure how I would answer the questions

Also AI often returns correct, but not blank responses
(ie it puts work in)

Of course a better system prompt and model will fix these issues